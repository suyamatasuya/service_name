<!DOCTYPE html>
<html>
  <head>
    <title>ケア方法</title>
    <link
      rel="stylesheet"
      href="https://use.fontawesome.com/releases/v5.15.3/css/all.css"
      integrity="sha384-SZXxX4whJ79/gErwcOYf+zWLeJdY/qpuqC4cor4uKdmFDea5575J6V4pV1Bk4Kx5"
      crossorigin="anonymous"
    >
    <style>
      /* 全体のスタイル */
      body {
        font-family: Arial, sans-serif;
        background-color: #f8f9fa;
        margin: 0;
        padding: 0;
      }

      /* ケア方法のコンテナ */
      .care-methods {
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        padding: 20px;
      }

      /* カードのスタイル */
      .card {
        width: 300px;
        background-color: #fff;
        border-radius: 10px;
        box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
        padding: 20px;
        margin-bottom: 20px;
      }

      /* タイトルのスタイル */
      .card-title {
        font-size: 20px;
        font-weight: bold;
        color: #333;
        margin-bottom: 10px;
        text-align: center;
      }

      /* 説明文のスタイル */
      .card-text {
        font-size: 16px;
        color: #555;
        margin-bottom: 10px;
        text-align: center;
      }

      /* リンクのスタイル */
      .card-link {
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 16px;
        color: #007bff;
        text-decoration: none;
        margin-top: 10px;
      }

      .card-link i {
        margin-left: 5px;
      }
    </style>
  </head>
  <body>
    <div class="care-methods">
      <div class="card">
        <h5 class="card-title">ケア方法</h5>
        <% if @care_methods && @care_methods.any? %>
          <% @care_methods.each do |method| %>
            <h5 class="card-title"><%= method.name %></h5>
            <p class="card-text"><%= method.description %></p>
            <% if method.link.present? %>
              <a href="<%= method.link %>" class="card-link" target="_blank">
                詳細リンク
                <i class="fas fa-external-link-alt"></i>
              </a>
            <% end %>
          <% end %>
        <% else %>
          <p class="card-text">No care methods found.</p>
        <% end %>
      </div>
    </div>
  </body>
</html>
