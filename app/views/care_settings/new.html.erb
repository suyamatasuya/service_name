<%= stylesheet_link_tag 'care_settings_new' %>

<div class="container text-center mt-6">
  <h1 class="mb-4">ケア時間設定</h1>

  <div class="step-section">
    <h2 class="step-title">Step 1: LINE友達追加</h2>
    <p>最初に、下のQRコードを使用して、公式LINEアカウントを友達追加してください。</p>
    <div class="line-qr-section text-center mt-3">
      <%= image_tag '310msgda.png', alt: 'LINE QR Code', class: 'line-qr-code' %>
      <p class="mt-2">公式LINEの追加お願いします</p>
    </div>
  </div>

  <div class="step-section mt-5">
    <h2 class="step-title">Step 2: ケア時間を設定</h2>
    <p>友達追加が完了したら、以下からケア時間を設定してください。</p>
    <%= form_for @care_setting, html: { class: 'care-setting-form' } do |f| %>
      <div class="form-group mb-3">
        <%= f.label :morning_care_time, "午前のケア時間", class: "form-label" %>
        <%= f.select :morning_care_time, (5..11).to_a.map { |hour| [hour.to_s.rjust(2, '0') + ":00", "#{hour}:00:00"] }, {}, class: "form-control" %>
      </div>

      <div class="form-group mb-3">
        <%= f.label :evening_care_time, "午後のケア時間", class: "form-label" %>
        <%= f.select :evening_care_time, (12..23).to_a.map { |hour| [hour.to_s.rjust(2, '0') + ":00", "#{hour}:00:00"] }, {}, class: "form-control" %>
      </div>

      <%= f.submit "設定を保存", class: "btn btn-primary mt-3" %>
    <% end %>
  </div>
</div>
